front llama a una url del back. (carpeta routes)
utilizar estructura users.js para llamar "amigos" o para modificarlos modificanto el metodo.


login se hace metodo post.
En el html el formulario puede tener una estructura similar a la siguiente para que funcione
<form action="http://localhost:3000/newUser" method="post">
	<...>
    <input type="submit" class="btn btn-primary">enviar/guardar</input>
</form>
Este input con aspecto de botón hace directamente un post al pulsarlo

if usuario no valido. err

else trae feed usurio requerido.



en el backend definimos metodo get para traer datos
que se leen los datos con fecht. en un js relaccionado con un html.